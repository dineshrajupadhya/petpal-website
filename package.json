{
  "name": "petpal-platform",
  "version": "1.0.0",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix server"
  }
}
